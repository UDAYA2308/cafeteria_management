<%= form_with(model: status_code, local: true) do |form| %>
  <% if status_code.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(status_code.errors.count, "error") %> prohibited this status_code from being saved:</h2>

      <ul>
        <% status_code.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
